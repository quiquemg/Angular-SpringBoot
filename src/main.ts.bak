import { enableProdMode } from '@angular/core';
import { platformBrowserDynamic } from '@angular/platform-browser-dynamic';

import { AppModule } from './app/app.module'; // Importa el módulo principal de la aplicación
/*import { environment } from './environments/environment'; // Importa el entorno de la aplicación

if (environment.production) {
  enableProdMode(); // Habilita el modo de producción si estamos en producción
}*/

/*
platformBrowserDynamic().bootstrapModule(AppModule)
  .catch(err => console.error(err));
*/